name: kasirmadura
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  dio: ^5.7.0          # <— tambahkan ini
  flutter_svg: ^2.0.10+1  # <— pindahkan dari dev_dependencies ke sini
  # http: ^1.2.2       # <— kalau sudah pakai Dio, ini bisa DIHAPUS agar tidak dobel
  dotted_border: ^2.0.0+3
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo3.png
